# Copy this file to .env and fill in the required values before running:
#   cp .env.example .env
#   docker compose up -d

# ─── Required ────────────────────────────────────────────────────────────────

# Public URL of the app (no trailing slash). Used in email links and SAML metadata.
BASE_URL=http://localhost:8080

# Secret used to sign AND encrypt session cookies. Generate with:
#   openssl rand -base64 32
# Minimum 32 characters — the server refuses to start below that. The signing
# and encryption keys are derived from this one value, so there is nothing else
# to configure. Changing it logs everyone out.
SESSION_SECRET=change-me-in-production-this-is-really-insecure

# Secret used to sign JWTs for API bearer tokens. Generate with: openssl rand -base64 32
JWT_SECRET=change-me-in-production

# ─── Email / SMTP (optional — notifications disabled when unset) ──────────────

# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=user@example.com
# SMTP_PASSWORD=secret
# SMTP_FROM=helpdesk@example.com

# ─── Feature flags (optional) ────────────────────────────────────────────────

# GUEST_SUBMISSION_ENABLED=false
# SLA_ENABLED=false

# Credential attempts allowed per client address per minute, across login,
# MFA verification, signup and MFA enrolment confirmation. 0 disables it.
# Counted per process and per source address: behind a reverse proxy that does
# not preserve the client address this throttles per proxy, which is stricter
# than intended rather than weaker.
# AUTH_RATE_LIMIT_PER_MINUTE=10
# MFA_ENABLED=false

# ─── Logging (optional) ──────────────────────────────────────────────────────

# Log level: debug, info, warn, error (default: info)
# LOG_LEVEL=debug
